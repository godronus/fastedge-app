{
  "name": "FastEdge Application Dev Environment",
  // Use base image + features for flexibility
  // "image": "ghcr.io/godronus/fastedge-app/devcontainer:latest",
  "image": "mcr.microsoft.com/devcontainers/base:debian",

  // Features are installed during prebuild (once)
  // Users get instant startup because prebuild has features cached
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "24",
      "installYarnUsingApt": false
    },
    "ghcr.io/devcontainers/features/rust:1": {
      "version": "latest",
      "profile": "minimal",
      "targets": "wasm32-wasip1"
    },
    "ghcr.io/devcontainers/features/github-cli:1": {
      "version": "latest"
    },
    "ghcr.io/devcontainers/features/docker-in-docker:2": {
      "version": "latest",
      "moby": false
    }
  },

  "onCreateCommand": "bash .devcontainer/setup.sh",
  "postAttachCommand": "bash .devcontainer/start.sh",

  "customizations": {
    "vscode": {
      "extensions": [
        "rust-lang.rust-analyzer",
        "tamasfe.even-better-toml",
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode"
      ],
      "settings": {
        "rust-analyzer.checkOnSave.command": "clippy",
        "rust-analyzer.cargo.target": "wasm32-wasip1",
        "editor.formatOnSave": true,
        "mcp.servers": {
          // "fastedge": {
          //   "command": "npx",
          //   "args": ["@your-company/fastedge-mcp"]
          // }
        },
        "workbench.editorAssociations": {
          "README.md": "vscode.markdown.preview.editor"
        }
      }
    }
  },

  "secrets": {
    "GCORE_API_TOKEN": {
      "description": "Enter your GCore API key for MCP server access"
    }
  },

  "remoteUser": "vscode",
  "forwardPorts": [8181]
}
